const baseURL = "http://localhost:3000";
const getColumns = `${baseURL}/columns`;
const getCards = `${baseURL}/cards`;
const changeCards = `${baseURL}/change-cards`;

export { getColumns, getCards, changeCards };
